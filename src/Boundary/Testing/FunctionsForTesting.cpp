#include "pch.h"
#include "Filesystem.h"

/* void renameSettingsFile(const std::string& newName) {
	std::filesystem::path folderPath = GetSettingsPath();
	std::filesystem::rename(folderPath / "Settings.json", folderPath / newName);
 } 

  void copyTestFileIntoSettings(const std::string& fileName) {
	 std::filesystem::path folderPath = GetSettingsPath();
	 std::filesystem::rename(folderPath / "Settings.json", folderPath / newName);
 } */

